# StudyTrack Pro - Redis multi-database
# DB 1: cache (Laravel cache, rate limit)
# DB 2: queue (Horizon, jobs)
# DB 3: session (sessões)
# DB 4: horizon (Horizon state)
maxmemory 256mb
maxmemory-policy allkeys-lru
appendonly no
