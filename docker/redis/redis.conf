# Redis configuration — password set via REDIS_PASSWORD env at startup

bind 0.0.0.0
protected-mode yes
port 6379

tcp-keepalive 300
timeout 0
maxclients 10000

maxmemory 512mb
maxmemory-policy allkeys-lru

save 900 1
save 300 10
save 60 10000

loglevel notice
logfile ""
appendonly no
