# Plano de Limpeza — Sobreposição Graficos vs Rotas Existentes

## Resumo

A nova página Graficos (`/graficos`) tornou several componentes existentes redundantes. Este plano remove código morto, consolida duplicatas e simplifica o dashboard mantendo compatibilidade.

---

## Fase 1 — Remover Código Morto (RISCO BAIXO)

### 1.1 Deletar HeatmapWidget.vue
- **Arquivo:** `frontend/src/features/dashboard/components/HeatmapWidget.vue` (178 linhas)
- **Risco:** NENHUM — grep confirma zero imports fora do próprio arquivo
- O dashboard já não usa este widget

### 1.2 Deletar PieChart.vue
- **Arquivo:** `frontend/src/components/charts/PieChart.vue` (226 linhas)
- **Risco:** NENHUM — TechDistributionPanel usa DonutChart, não PieChart

### 1.3 Limpar tipos
- **Arquivo:** `frontend/src/types/chart.types.ts` — remover `PieChartData` se não usado

---

## Fase 2 — Remover ReportsView (RISCO MÉDIO)

### 2.1 Atualizar rota `/reports`
- **Arquivo:** `frontend/src/router/routes/reports.routes.ts`
- Substituir import do ReportsView por redirect para `/graficos`

### 2.2 Atualizar rota `/settings/reports`
- **Arquivo:** `frontend/src/router/routes/settings.routes.ts`
- Redirect `/settings/reports` → `/graficos`

### 2.3 Remover nav item "Relatórios"
- **Arquivo:** `frontend/src/constants/sidebar-nav.ts`
- Remover entrada 'Relatórios' (mantém só 'Gráficos')

### 2.4 Deletar ReportsView.vue
- **Arquivo:** `frontend/src/views/reports/ReportsView.vue` (441 linhas)
- **Risco:** BAIXO após rotas atualizadas — sem imports restantes

---

## Fase 3 — Simplificar Dashboard Widgets (RISCO MÉDIO)

### 3.1 Simplificar TechDistributionWidget.vue
- **Arquivo:** `frontend/src/features/dashboard/components/TechDistributionWidget.vue` (491 → ~150 linhas)
- Converter para card de resumo simples: top 5 tecnologias com barras de progresso
- Remover ApexCharts polar chart, BarChart import, lógica de viewport responsivo
- Adicionar link "Ver gráficos →" para `/graficos`

### 3.2 Manter TimeSeriesWidget e WeeklyComparisonWidget
- Decisão: MANTER — servem como resumos leves do dashboard
- Dashboard ≠ página de analytics completa

---

## Fase 4 — Verificação (RISCO BAIXO)

- Verificar DashboardView não importa HeatmapWidget (já verificado)
- Nenhuma mudança necessária no DashboardView

---

## Ordem de Implementação

```
Fase 1 (Código Morto) → Fase 2 (ReportsView) → Fase 3 (Simplificar) → Fase 4 (Verificar)
     ↓                        ↓                        ↓                      ↓
  Deletar 2 arquivos      Atualizar 3 arquivos     Reescrever 1         Rodar testes
  (HeatmapWidget,         (rotas, sidebar)         (TechDistWidget)
   PieChart)
```

**Esforço estimado:** 2-3 horas
**Arquivos modificados:** 4
**Arquivos deletados:** 3
**Linhas removidas:** ~850+
**Linhas simplificadas:** ~340 (TechDistWidget 491→150)

---

## Arquivos

### DELETAR
1. `frontend/src/features/dashboard/components/HeatmapWidget.vue` (178 linhas)
2. `frontend/src/components/charts/PieChart.vue` (226 linhas)
3. `frontend/src/views/reports/ReportsView.vue` (441 linhas)

### MODIFICAR
1. `frontend/src/router/routes/reports.routes.ts` — redirect `/reports` → `/graficos`
2. `frontend/src/router/routes/settings.routes.ts` — redirect `/settings/reports` → `/graficos`
3. `frontend/src/constants/sidebar-nav.ts` — remover 'Relatórios'
4. `frontend/src/features/dashboard/components/TechDistributionWidget.vue` — simplificar para ~150 linhas

### SEM MUDANÇAS
- `DashboardView.vue` — já não usa HeatmapWidget
- `GraficosView.vue` — sem mudanças
- Todos os componentes Graficos — sem mudanças
- Backend API — sem mudanças

---

## Verificação

1. `npx vue-tsc --noEmit` — sem erros novos
2. `npx vitest run` — testes existentes passam
3. Navegar `/reports` → redireciona para `/graficos`
4. Navegar `/settings/reports` → redireciona para `/graficos`
5. Sidebar mostra só "Gráficos", não "Relatórios"
6. Dashboard carrega com TechDistWidget simplificado
7. Sem erros no console
