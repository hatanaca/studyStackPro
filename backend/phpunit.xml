<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory>app/Exceptions</directory>
        </exclude>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="127.0.0.1"/>
        <env name="DB_PORT" value="5432"/>
        <env name="DB_DATABASE" value="studytrack_test"/>
        <env name="DB_USERNAME" value="studytrack"/>
        <env name="DB_PASSWORD" value="secret"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="BROADCAST_CONNECTION" value="log"/>
        <!-- force="true": variáveis do .env (ex.: REDIS_HOST=redis, REDIS_PASSWORD do Docker) não podem ficar ativas nos testes. -->
        <env name="REDIS_CLIENT" value="predis" force="true"/>
        <env name="REDIS_HOST" value="127.0.0.1" force="true"/>
        <env name="REDIS_PORT" value="6379" force="true"/>
        <!-- Vazio = Redis local / CI (sem requirepass). Com Docker na raiz: REDIS_PASSWORD=... php artisan test -->
        <env name="REDIS_PASSWORD" value="" force="true"/>
        <!-- Sem Redis alinhado ao phpunit, o rate limit Lua falha e devolvia 503 em quase todas as rotas. -->
        <env name="RATE_LIMIT_FAIL_OPEN" value="true"/>
        <env name="REVERB_APP_ID" value="test"/>
        <env name="REVERB_APP_KEY" value="test-key"/>
        <env name="REVERB_APP_SECRET" value="test-secret"/>
        <env name="SANCTUM_STATEFUL_DOMAINS" value="localhost,127.0.0.1,localhost:5173,127.0.0.1:5173" force="true"/>
    </php>
</phpunit>
